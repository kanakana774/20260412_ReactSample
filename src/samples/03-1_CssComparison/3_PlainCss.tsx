import "./style.css";

export default function PlainCss() {
  return (
    <div className="plain-card">
      <h4 className="plain-title">3. 普通のCSS</h4>
      <p>外部.cssをimportしてclassNameで指定します。</p>
    </div>
  );
}
